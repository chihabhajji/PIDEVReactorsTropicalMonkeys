    {% extends 'back/baseback.html.twig' %}
    {% block title %}Ajout d'une association{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="card-group">
<div class="col-lg-150 col-md-200" >
    <div class="card">
        <div class="card-body">
            <h1>Associations list :</h1>

            <h1>Création d'une nouvelle association :</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('association_index') }}">Retour vers la liste des associations</a>
        </li>
    </ul>
        </div>
    </div>
</div>
    </div>
</div>

{% endblock %}

